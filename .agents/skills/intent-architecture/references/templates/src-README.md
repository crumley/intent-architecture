# <System> Source

The **code** leg. Implements the realization described in [`../design/`](../design/), under the
constraints set by [`../intent/`](../intent/).

> **Status:** <placeholder until the first implementation pass | active>. Lay out source here to
> **mirror the design's own organization** (whatever grouping the build follows). The concrete
> module layout is itself a **design** decision — record it in the design entry that populates this
> tree, along with the language toolchain (as ADRs) and its checks in the gate (see
> [`../CONTRIBUTING.md`](../CONTRIBUTING.md)).

`src` moves with `design` and `test`; all three reconcile back to `intent` when they diverge.
