# NNNN — <decision title>

> **Status:** proposed | accepted | superseded by `NNNN-<title>.md` · **Date:** YYYY-MM-DD

An Architecture Decision Record. Copy this file to `NNNN-short-title.md` (next number in sequence)
for each critical **stack / tooling** choice — the one-time, cross-cutting decisions (language,
runtime, test runner, key libraries). The toolchain, each framework, and each significant library
gets its own ADR; rationale is a primary output of this repo. A design entry
([`../README.md`](../README.md)) links the ADRs it rests on; an ADR does not describe a unit of
build work.

## Context

What forces are at play? What does the intent require here (link the slice), and what is left to the
build? What problem are we deciding about?

## Options considered

- **Option A** — …
- **Option B** — …
- **Option C** — …

For each, the honest tradeoffs — not just the winner's virtues.

## Decision

What we chose.

## Why

The reasoning, tied back to the intent and its organizing lens. This is the part future readers will
care about most.

## Consequences

What this makes easy, what it makes hard, what it commits us to, and how reversible it is. Note any
follow-on decisions it forces.
